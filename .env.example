# Copy to .env.local for local testing (optional).
# Set the same key in Vercel → Project → Settings → Environment Variables.

# Google Analytics 4 Measurement ID (e.g. G-XXXXXXXXXX)
NEXT_PUBLIC_GA_MEASUREMENT_ID=
